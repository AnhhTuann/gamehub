version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    container_name: omniwear_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: tuan2525
      POSTGRES_DB: omniwear
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
